import { FormEvent, useState } from "react";
import { ArrowLeft, ArrowRight, LockKeyhole, Mail, ShieldCheck } from "lucide-react";
import { Link, useLocation } from "wouter";
import { BrandLogo } from "@/components/BrandLogo";
import { supabaseConfigured, supabase } from "@/lib/supabase";
export default function Login() { const [, navigate] = useLocation(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [mode, setMode] = useState<"login" | "signup">("login"); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const submit = async (event: FormEvent) => { event.preventDefault(); setMessage(""); if (!supabase) { setMessage("Supabase ainda não está configurado. Preencha o .env antes de entrar."); return; } setBusy(true); const result = mode === "login" ? await supabase.auth.signInWithPassword({ email, password }) : await supabase.auth.signUp({ email, password, options: { data: { full_name: email.split("@")[0] } } }); setBusy(false); if (result.error) setMessage(result.error.message); else if (mode === "signup") setMessage("Conta criada. Verifique seu e-mail para confirmar o acesso."); else navigate("/admin"); }; return <div className="min-h-screen bg-[#f7f5f0] text-[#183039]"><div className="mx-auto grid min-h-screen max-w-7xl lg:grid-cols-[.78fr_1.22fr]"><section className="hidden flex-col justify-between bg-[#14252c] p-10 text-white lg:flex"><BrandLogo inverse /><div><div className="mb-6 grid h-14 w-14 place-items-center rounded-2xl bg-[#d7f56c] text-[#183039]"><LockKeyhole className="h-6 w-6" /></div><h1 className="font-display max-w-md text-5xl font-semibold leading-[.98] tracking-[-.07em]">Seu painel, suas páginas, seu ritmo.</h1><p className="mt-6 max-w-md text-sm leading-7 text-[#b5c7bf]">Gerencie todos os negócios em um único lugar — com segurança, clareza e espaço para crescer.</p></div><p className="text-xs text-[#829c90]">Página Inteligente · gestão de presença digital</p></section><section className="flex items-center px-5 py-10 sm:px-10"><div className="mx-auto w-full max-w-md"><div className="mb-10 lg:hidden"><BrandLogo /></div><Link href="/" className="mb-10 inline-flex items-center gap-2 text-xs font-bold text-[#779180]"><ArrowLeft className="h-4 w-4" />Voltar para início</Link><p className="text-xs font-bold uppercase tracking-[.2em] text-[#769663]">Área administrativa</p><h2 className="font-display mt-3 text-4xl font-semibold tracking-[-.06em]">{mode === "login" ? "Bem-vindo de volta." : "Crie seu acesso."}</h2><p className="mt-3 text-sm leading-6 text-[#7c8982]">{mode === "login" ? "Entre para cuidar das páginas dos seus clientes." : "Seu primeiro acesso ficará pendente de aprovação."}</p>{!supabaseConfigured && <div className="mt-6 rounded-2xl border border-[#f0dfb4] bg-[#fff6df] p-4 text-sm leading-6 text-[#866b2a]"><strong>Ambiente não conectado.</strong> A tela está pronta, mas a autenticação real depende das variáveis do Supabase.</div>}<form onSubmit={submit} className="mt-8 space-y-4"><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[.12em] text-[#5f756c]">E-mail</span><div className="relative"><Mail className="absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-[#9aa79f]" /><input type="email" required value={email} onChange={e => setEmail(e.target.value)} placeholder="voce@empresa.com" className="h-13 w-full rounded-2xl border border-[#dfe4da] bg-white pl-11 pr-4 text-sm outline-none transition focus:border-[#9dbd7b] focus:ring-4 focus:ring-[#d7f56c]/20" /></div></label><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[.12em] text-[#5f756c]">Senha</span><div className="relative"><ShieldCheck className="absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-[#9aa79f]" /><input type="password" required minLength={6} value={password} onChange={e => setPassword(e.target.value)} placeholder="Mínimo de 6 caracteres" className="h-13 w-full rounded-2xl border border-[#dfe4da] bg-white pl-11 pr-4 text-sm outline-none transition focus:border-[#9dbd7b] focus:ring-4 focus:ring-[#d7f56c]/20" /></div></label>{message && <div className="rounded-2xl border border-[#e6d2c7] bg-[#fff1ec] px-4 py-3 text-sm leading-6 text-[#935f4d]">{message}</div>}<button disabled={busy} className="brand-button flex h-13 w-full items-center justify-center gap-2 rounded-full bg-[#14252c] text-sm font-bold text-white disabled:opacity-60">{busy ? "Aguarde..." : mode === "login" ? "Entrar no painel" : "Criar conta"}<ArrowRight className="h-4 w-4" /></button></form><button onClick={() => { setMode(mode === "login" ? "signup" : "login"); setMessage(""); }} className="mt-6 w-full text-center text-sm font-semibold text-[#69876f] hover:text-[#183039]">{mode === "login" ? "Ainda não tenho acesso" : "Já tenho uma conta"}</button></div></section></div></div> }
